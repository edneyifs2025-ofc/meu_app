import 'package:flutter/material.dart';

class FrequenciaPage extends StatelessWidget {
  const FrequenciaPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Presença: 90%', style: TextStyle(fontSize: 22)),
    );
  }
}
